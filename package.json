{
  "name": "lovemenu-miniprogram",
  "version": "1.0.0",
  "private": true,
  "description": "LoveMenu 原生微信小程序",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check-project.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
